credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          description: "Github token"
          id: "github-token"
          username: "nicokosi"
          password: "${GITHUB_TOKEN}"
          scope: GLOBAL
